// Ambient declaration for global CSS imports (e.g. `import "./globals.css"`
// in src/app/layout.tsx). Next.js handles the actual CSS at build time —
// this is purely to tell the TypeScript language server that the import
// is valid and avoid a "Cannot find module" diagnostic in editors.
declare module "*.css";
